import { renderLetter } from './letter.util';
export type * from './letter.types';
export declare const letterUtil: {
    LETTERS: import("./letter.types").Letters;
    LETTER_NAME: string;
    renderLetter: typeof renderLetter;
};
